interface:
  display_name: "Academic Search"
  short_description: "Search papers with traceable metadata and PDF checks"
  default_prompt: "Use $academic-search to search papers across disciplines, compare sources, check open-access evidence, and extract structured metadata with source and verification status."
