interface:
  display_name: "Academic Search"
  short_description: "Search and validate papers with traceable evidence"
  default_prompt: "Use $academic-search to search papers across disciplines, compare sources, validate or deduplicate scholarly records, check open-access evidence, and extract structured metadata with source and verification status."
